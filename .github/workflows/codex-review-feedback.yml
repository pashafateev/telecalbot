name: Codex review feedback

on:
  workflow_run:
    workflows: ["Codex review"]
    types: [completed]

jobs:
  post_feedback:
    permissions:
      actions: read
      issues: write
      pull-requests: write
    uses: pashafateev/codex-workflows/.github/workflows/codex-review-feedback.yml@2e17d40cd2716d8bf41d1ccef292de74e6b63511 # v1.0.0
