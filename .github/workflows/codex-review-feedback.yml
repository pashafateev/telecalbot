name: Codex review feedback

on:
  workflow_run:
    workflows: ["Codex review"]
    types: [completed]

jobs:
  post_feedback:
    permissions:
      actions: read
      issues: write
      pull-requests: write
    uses: pashafateev/codex-workflows/.github/workflows/codex-review-feedback.yml@v1
