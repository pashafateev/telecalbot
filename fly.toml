app = "telecalbot"
primary_region = "fra"

[env]
DATABASE_PATH = "/data/telecalbot.db"

[processes]
app = "/app/.venv/bin/python -m app.main"

[[mounts]]
source = "telecalbot_data"
destination = "/data"
